export interface LoanBookResponseDto {
  // Add properties for loanBook response
  id: string,
  bookId: string,
  userId: string,
  loanDate: Date,
  dueDate: Date,
  createdAt: Date,
  updatedAt: Date,
}
